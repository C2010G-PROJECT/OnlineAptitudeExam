const _AJAX_PREFIX = "/Ajax";
const _PREFIX = "/Admin";

const ContentBody = "#contentBody";
const ContentPager = "#contentPager";

function go(delta = 1) {
    history.go(delta)
}

function back() {
    history.back();
}